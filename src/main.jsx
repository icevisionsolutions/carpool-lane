import React from "react";
import ReactDOM from "react-dom/client";
import CarpoolApp from "./CarpoolApp.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <CarpoolApp />
  </React.StrictMode>
);
